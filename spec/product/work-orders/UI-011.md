UI-011 — Protocol port re-anchoring to the composed runtime

Status: PLANNED (dispatchable — the hard cross-program dependency RTN-012 is merged: f22a624)
Depends on: RTN-012 (merged; the composed protocol runtime on main: kernel, A15 evidence, A16 risk, A01–A12 authorities, A13/A14 rails, gateway, transition runtime, hosting)
Dependents: UI-010
Owned surfaces: src/lib/protocol/ port backings — every *-port.ts and *-state-mapping.ts as they exist at execution time; the stand-in authority mocks (mock-*-authority.ts — retirement); new runtime adapter modules colocated in src/lib/protocol/; spec/product/*-mapping-records.md (re-anchoring to runtime truth); the ONE gateway boundary-test splice-guard expectation update described below
Forbidden: changes to any port interface consumed by product surfaces (the adapter boundary shapes freeze); changes to product surfaces (src/app/, src/components/ — read-only consumers); protocol semantics or any src/lib/protocol-runtime/ source (read-only consumption via the composed barrel), with the single sanctioned exception of the gateway boundary test's splice-guard expectation (below); spec/architecture/v0.1/; re-implementing any authority semantics in the product layer

Objective

Replace every NON-AUTHORITATIVE mock port backing with a runtime adapter over the composed protocol runtime, retire the mocks, and re-anchor every product mapping record to runtime truth — the sanctioned product splice (rtn-plan-rulings.md Q5/delta 6). Commands flow exclusively through ProtocolGateway.submitCommand (COMMAND-SURFACE.md: "the ONLY function other code may call to submit a protocol command"); state presentation reads the runtime's documented read surface (authority public query APIs + the A15 evidence log for proof trails). RTN-012's composed-journey question 9 defers to this item: the user-visible-state reconciliation lands here (spec/governance/dogfooding-protocol.md, nine questions, answered against runtime truth).

Acceptance

Every port (intent, checkout, capability, tracking, waiting, liquidity, mediation — each *-port.ts as it exists at execution time) is backed by a runtime adapter: commands go through ProtocolGateway.submitCommand with a kernel CommandEnvelope; state reads use the runtime's public read surface; nothing in the product layer fabricates authority state, acceptance, or settlement wording (N1, N3, N5 discipline unchanged).
All mock authority backings are retired (the mock-*-authority.ts files are deleted); no NON-AUTHORITATIVE or PRESENTATION-ONLY backing remains; the boundary() reports describe the runtime adapter honestly (implementation identity, capability limits).
Port interfaces are unchanged: the function signatures product surfaces call today compile unchanged (typecheck proves it); the mock fixture vocabularies are replaced by the real authority state vocabularies as exported by the runtime's own modules; each *-state-mapping.ts maps the real vocabulary to the six display states with UNKNOWN never worded or styled as success or failure (P4/P5).
Every renderable state through every port carries a re-anchored nine-question mapping record in spec/product/*-mapping-records.md, answered against runtime truth (owning authority, evidence identity, UNKNOWN semantics, reconciliation path, user-visible wording source); unmapped states do not ship; RTN-012's question-9 deferral is discharged and cross-referenced.
The gateway boundary test's splice guard (src/lib/protocol-runtime/gateway/boundary.test.ts, "the product splice is UI-011, not RTN-010") is updated honestly in this work item: src/lib/protocol/ port adapters are the sanctioned importers of protocol-runtime (via the composed barrel); src/app/ and src/components/ still never import it — mechanically enforced, invariant unchanged.
Gateway admission outcomes surface correctly in the product: DUPLICATE receipts and typed rejections (including COMPLIANCE_BLOCKED and validation rejections with reason codes) present as explicit states through the mapping discipline; no silent drops; no retry storms.
Durable behavior: submitted commands are recorded (receipts persisted per the gateway's own store); UNKNOWN external outcomes render as UNKNOWN with the reconciliation path the runtime actually executes (the A14 cycle / GC-2 semantics) — not invented product-side wording.
Full verification: bunx tsc --noEmit = 0; bun test green (the product suites updated to the runtime-backed ports; the runtime's own 1901-test suite unaffected); all 13 plain-Node harnesses exit 0 unchanged; bun run build green; python3 scripts/validate_governance.py exit 0.

Required evidence

Per-port adapter verification (command round-trips through the gateway, state reads from the runtime, UNKNOWN presentation); per-port mock-retirement proof (deletion list); per-mapping-record-file re-anchoring summary (states mapped, questions answered, deferrals none-or-recorded); the boundary-test splice-guard amendment diff; full battery outputs.

Stop conditions

Any port interface that cannot be preserved against the runtime surface (report the gap — do not bend the interface); any state that cannot be mapped without a semantic change; any runtime read-surface gap for a port's presentation needs; any evidence-chain question the runtime cannot answer (record it as a deferral to SYS-001, never silently).

Integration notes for the worker

Read src/lib/protocol-runtime/gateway/COMMAND-SURFACE.md FIRST — it is this item's declared input (submission contract, response contract, health, the 112-kind catalogue with per-kind body schemas and subject resolution).
Read src/lib/protocol-runtime/INTEGRATION-EVIDENCE.md (RTN-012): the composed golden path, the nine questions as answered at the protocol/runtime boundary, and the question-9 deferral this item discharges.
The composed barrel is src/lib/protocol-runtime/index.ts (188 exports; Node-loadable; composition order documented). The runtime is in-process: adapters instantiate/compose it per the barrel's documented composition order (substrate → evidence → authorities → persist hooks → bindings → transition → gateway → scheduler), or consume a server-side singleton the repo already documents — never invent a second composition root.
Parallel-sibling rule: no other item is in flight against these surfaces; base exactly on main @ b0142c3f99411d62bee586e7989bfa386dfb5c46 and record it.
