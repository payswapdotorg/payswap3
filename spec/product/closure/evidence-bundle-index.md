# UI-010 — the evidence bundle index

Every artifact this closure submission relies on, where it lives, and the exact
command that produced it. All commands run from `spec/product/closure/tools/`
unless noted; the app leg runs against the built standalone server
(`bun run build` from the repository root, then
`cd .next/standalone && PORT=3210 HOSTNAME=127.0.0.1 node server.js`, with
`.next/static` copied into `.next/standalone/.next/` per the standalone output's
documented preparation). Environment: sandbox (`PAYSWAP_ENV` unset); Node
v24.19.0; Playwright 1.62 (Chromium); axe-core 4.13 (from
`hardening/tools/node_modules`, the same version the UI-009 evidence used).

## The plan

| Artifact | Producing command |
|---|---|
| `../evidence-plan.md` | written before execution (UI-010 implementation step 1) |

## Leg A — the port-level end-to-end workflow journeys (the runtime-backed adapters over the real composed runtime)

| Artifact | Producing command |
|---|---|
| `workflows/port-journeys.json` (+ console transcript `workflows/port-journeys-transcript.txt`) | `node --import ./alias-loader.mjs product-port-journeys.mjs` — composes the runtime exactly as `src/lib/protocol/server-runtime.ts` does (journey-lib.mjs; two recorded deviations: a fresh `var/ui010-journeys/` dir, and the tick-based drain the composed-journey harness uses), registers the seven runtime adapters into the product ports, and drives WF-1..WF-6 through the PRODUCT PORTS. 62 assertions / 0 failed; A15 chain VERIFIED (height 29). |
| `workflows/port-journeys.md` | `node gen-journey-md.mjs` — renders the dogfooding-protocol journey records (steps with state before/after, assertions, the nine reconciliation questions per workflow) from the JSON. |
| `workflows/probe-drain.txt` | `node --import ./alias-loader.mjs probe-drain.mjs` — FINDING 1's probe: the server composition's `drain()` (= `worker.stop()`) does not execute the just-admitted command; the tick control does. |

## Leg B — the built app over HTTP + browser (the app as it is)

| Artifact | Producing command |
|---|---|
| `app-e2e/app-e2e.json` + `app-e2e.md` | `node app-e2e-audit.mjs` (BASE_URL=http://localhost:3210) — B-0 the findings evidenced live; B-1 environment signal + anti-spoof; B-2 the role matrix (168 deep-link cells + record-content leakage + the mediation record API viewer resolution); B-3 WF-7 browser-context honest UNKNOWN; B-4 the app's actual server-rendered presentations; B-5 the dispute-initiation flow; B-6 navigation leakage. 31 checks / 0 failed. |
| `app-e2e/wf7-pay-review-unknown.png`, `app-e2e/wf7-intent-state-unknown.png`, `app-e2e/dispute-initiation-consequences.png` | captured by `app-e2e-audit.mjs` (Playwright full-page screenshots of the honest UNKNOWN presentations and the fail-closed initiation surface). |
| `app-e2e/environment-signal.json` | `node env-signal-check.mjs` — the controlled configuration check (a second instance with `PAYSWAP_ENV=production`): /api/health + dynamic pages derive production; static pages record FINDING 4. |
| `app-e2e/findings.md` | written from the recorded artifacts above (the five findings' full analysis + the stop-condition assessment). |

## Responsive + accessibility re-verification (the composed system, fresh)

| Artifact | Producing command |
|---|---|
| `a11y-responsive/a11y.json` | `node a11y-responsive-audit.mjs` — axe-core 4.13 (wcag2aa, wcag22aa, best-practice) + the semantics probe + the reduced-motion pass, per surface (28). |
| `a11y-responsive/keyboard.json` | same command — the tab walk per surface (coverage, traps, visible focus). |
| `a11y-responsive/responsive.json` | same command — 390/768/1440 per surface: hscroll + effective touch targets (pseudo-element hit areas included; the WCAG 2.5.8 exception classes classified and recorded separately). |
| `a11y-responsive/states.json` | same command — the six display states' distinct treatments on /state-primitives + the UNKNOWN frame honesty probe. |
| `a11y-responsive/rollup.md` | same command — the per-surface verdict table and the headline roll-up. |

## The mapping-completeness roll-up

| Artifact | Producing command |
|---|---|
| `mapping-audit.json` | `node --import ./alias-loader.mjs mapping-audit.mjs` — enumerates the consequential-state vocabularies from the display-resolution layer (`src/lib/protocol/*-state-mapping.ts`) + the frozen port types, parses all 91 records across the eight `spec/product/*-mapping-records.md` files for nine-answer completeness, and cross-checks. Result: 66 consequential states enumerated; 91/91 records nine-answer complete; boundary presentations verified present; **0 unmapped consequential states**. |

## Evidence honesty notes

1. **Reproducibility.** Every JSON/MD artifact above is regenerated by the
   listed command. The three PNG screenshots are committed renderings of the
   DOM state the scripts assert (the assertions, not the pixels, are the
   evidence; the pixels illustrate).
2. **Leg A vs Leg B.** Leg A drives the product's own port APIs over the real
   composed runtime (the seven runtime adapters, the gateway, the authorities,
   the A15 chain — the exact classes the app composes). Leg B drives the real
   built app. The two legs differ because of FINDING 2 (the splice does not
   reach the routes in the running app): the bundle evidences BOTH the
   adapters' correct behavior (Leg A) AND the app's actual honest behavior
   (Leg B). Nothing extrapolates one onto the other.
3. **Prior evidence.** The UI-009 bundle (`hardening/evidence/`) is cited as
   the prior per-surface a11y/responsive/role evidence; this bundle
   RE-VERIFIES the same standard on the UI-011-re-anchored composed system
   rather than rolling the prior evidence forward unchanged, because UI-011
   changed every state presentation.
4. **The designated environment.** Sandbox. No run in this bundle presents
   production financial execution; the production-signal check is a
   configuration-derivation check on a separate instance, recorded as such in
   `app-e2e/environment-signal.json`.
