# build artifacts (DEP-002 packaging hygiene)
.next/
node_modules/
next-env.d.ts
__pycache__/
*.pyc
# local environment material — never source-controlled (S1/S4)
.env
.env.*
tsconfig.tsbuildinfo

# runtime-artifact directories (server-runtime.ts: "the repo's runtime-artifact
# directory, gitignored" — the documented intent; the entry was missing)
var/
